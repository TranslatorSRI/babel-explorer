# Valid CURIEs for integration tests.
# Add new CURIEs here to expand test coverage — tests are parametrized over this list.
MONDO:0004979
MONDO:0005044

# Parked, not deleted — see the TODO on the PR that parked it.
# NCIT:C55060 ("Hypertension, CTCAE") has no rows at all in Concord.parquet as of Babel
# 2026jul22, so every test parametrized over it asserted len(results) > 0 against an empty
# list. NodeNorm still resolves it — from Babel 2025sep1, with a clique of 2 — so the CURIE
# is not bogus; something changed between those releases. Restore this line once that is
# understood, rather than assuming the CURIE was simply wrong.
# NCIT:C55060
